# Build output
/.homeybuild/
node_modules/

# Secrets & keys
PRIVATE_KEY_BACKUP.txt
*.pem
*.key
test-config/

# Logs
output.log
*.log

# Debug/test scripts
test-*.js
test-*.py

# Python venv
pyvenv.cfg
bin/
lib/python*/
include/
__pycache__/

# Reference implementations (cloned separately)
QolsysController/
ha-qolsys-panel/

# OS
.DS_Store

# Note: app.json (the composed manifest) IS tracked. Both the Athom
# validate and publish actions require it to exist as the entry point —
# they don't auto-compose from .homeycompose/. Keep `app.json` and
# `.homeycompose/app.json` in sync manually (or via `homey app build`).
