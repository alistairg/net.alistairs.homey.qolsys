# Homey Driver: Qolsys IQ (IQ Remote Protocol)

## Source Reference
Protocol reverse-engineered by Eric Hylands. Reference implementations:
- Python module (MIT): https://github.com/EHylands/QolsysController
- HA integration: https://github.com/EHylands/ha-qolsys-panel

Both are cloned in the project root (`QolsysController/`, `ha-qolsys-panel/`).

---

## Architecture

The Qolsys panel runs a **built-in MQTT broker** secured with mutual TLS (mTLS). This app connects to that broker as an MQTT client, impersonating a virtual **IQ Remote** secondary keypad. No external broker required.

### Two Phases
1. **Pairing** — one-time TLS certificate exchange (Homey acts as TLS *server*, panel connects to Homey)
2. **Operation** — ongoing MQTT over mTLS using stored certificates

### Shared Client Pattern
One `QolsysPanelClient` per panel, shared across all devices (same pattern as the AVPro app). Managed in `app.ts`, referenced by both drivers.

---

## Project Structure

```
app.ts                              — QolsysApp: shared client + PkiManager lifecycle
.homeycompose/app.json              — app manifest source (edited here, NOT app.json)
app.json                            — GENERATED by `homey app build` — do not edit
lib/
  types.ts                          — enums & interfaces from QolsysController/enum.py
  PkiManager.ts                     — RSA keygen, CSR, cert storage (node-forge)
  PairingServer.ts                  — TLS server + mDNS for pairing exchange
  QolsysPanelClient.ts              — MQTT client, reconnection, arm/disarm commands
  DatabaseParser.ts                 — parse fulldbdata + apply dbChanged events
  ZoneTypes.ts                      — sensortype → Homey capability mapping + per-driver type lists
  ZoneDevice.ts                     — shared device class for every per-type zone driver
  ZoneDriver.ts                     — abstract driver class; subclasses just declare claimsZoneType
drivers/
  alarm-panel/                      — partition device (homealarm class)
    driver.ts                       — pairing flow, flow card registration
    device.ts                       — partition device, arm/disarm, state sync
    driver.compose.json
    driver.flow.compose.json
    pair/configure.html             — pair-flow intro page
    pair/pairing.html               — pairing progress + panel-side instructions
  contact-sensor/                   — Door_Window, Door_Window_M, Tilt → alarm_contact
  motion-sensor/                    — Motion, Panel Motion, Occupancy Sensor → alarm_motion
  smoke-detector/                   — SmokeDetector, Smoke_M → alarm_smoke
  co-detector/                      — CODetector → alarm_co
  water-sensor/                     — Water → alarm_water
  glass-break-detector/             — GlassBreak, Panel Glass Break → alarm_glass_break
    driver.ts                       — extends ZoneDriver, returns the sensor types it claims
    device.ts                       — re-exports ZoneDevice (no per-type subclass needed)
    driver.compose.json             — declares this driver's capabilities + name + images
    driver.flow.compose.json        — `<driver-id>_tampered` trigger (must be globally unique)
assets/icon.svg
locales/en.json
```

### Build & Run
```bash
npm install
npm run build           # tsc → .homeybuild/
homey app build         # compose + tsc + validate
homey app run           # deploy to Homey in dev mode
homey app install       # install for production
```

---

## Key Implementation Details

### TypeScript Imports
- **app.ts, driver.ts, device.ts**: `import Homey from 'homey'`
- **lib/*.ts files**: `import Homey from 'homey/lib/Homey'` (for the Homey instance type)

### PKI Storage
All PKI material stored in `homey.settings` with prefix `pki_` — **never** in `/userdata/` (publicly accessible via HTTP).

Keys: `pki_private_key`, `pki_certificate`, `pki_csr`, `pki_secure`, `pki_qolsys_ca`, `pki_mac_address`

PKI details (from `PkiManager.ts`):
- RSA 2048-bit, exponent 65537
- Subject: `C=US, ST=SanJose, L=, O=Qolsys Inc., CN=www.qolsys.com ` (trailing space on CN is intentional — matches Python source)
- Random MAC format: `f2:16:3e:xx:xx:xx` (4th byte 0x00-0x7F)

### Pairing (PairingServer.ts)
1. TLS server on random port 50000–55000
2. mDNS advertisement via `bonjour-service`: name `NsdPairService`, type `http`
3. Panel connects → `\x00\x11` + MAC exchange → CSR → receive .secure + .qolsys (terminated by `b"sent"`)
4. 5 minute timeout, cleanup on completion or error

### MQTT Connection (QolsysPanelClient.ts)
- Port 8883, `rejectUnauthorized: false`, `keepalive: 60`
- Client ID: `qolsys-controller-{formatted_mac}`
- Subscribe: `iq2meid`, `response_{formatted_mac}`
- Publish: `mastermeid`
- Post-connect sequence: `connect_v204` → `pingevent` → `pair_status_request` → `syncdatabase`
- Command correlation: UUID `requestID` in payload, response on `response_` topic
- Ping: `pingevent` every 600 seconds
- Reconnection: exponential backoff 5s → 10s → 20s → 40s → 80s → max 120s
- SSL errors are fatal (bad certs, re-pair required)

### Arm/Disarm Commands
Arm and disarm use IPC-wrapped commands (`ipcCall` eventName):
- `ipcServiceName`: `qinternalservice`
- `ipcInterfaceName`: `android.os.IQInternalService`
- `ipcTransactionID`: 7

Disarm logic varies by partition state:
- Alarm → `disarm_from_emergency`
- Exit delay → `disarm_from_openlearn_sensor`
- Armed → send `ui_delay` first, then `disarm_the_panel_from_entry_delay`

### Database (DatabaseParser.ts)
`fulldbdata` is an array of ContentProvider entries: `{ uri, resultSet[] }`.

Key URIs:
- `PartitionContentProvider/partition` — partition list
- `SensorContentProvider/sensor` — zone/sensor data (47 columns)
- `QolsysSettingsContentProvider/qolsyssettings` — system settings per partition
- `StateContentProvider/state` — runtime state (ALARM_STATE)

Live updates arrive as `dbChanged` events on `iq2meid`:
```json
{
  "eventName": "dbChanged",
  "dbOperation": "update",
  "uri": "content://...",
  "contentValues": { "field": "newValue" },
  "selectionArgs": ["zoneId", "partitionId"]
}
```

### Zone drivers (one per sensor category)
The 6 per-type drivers each handle a specific subset of Qolsys sensor types. The mapping is the source of truth for which driver claims which type during pairing.

| Driver | sensortypes claimed | Primary capability |
|---|---|---|
| `contact-sensor` | `Door_Window`, `Door_Window_M`, `Tilt` | `alarm_contact` |
| `motion-sensor` | `Motion`, `Panel Motion`, `Occupancy Sensor` | `alarm_motion` |
| `smoke-detector` | `SmokeDetector`, `Smoke_M` | `alarm_smoke` |
| `co-detector` | `CODetector` | `alarm_co` |
| `water-sensor` | `Water` | `alarm_water` |
| `glass-break-detector` | `GlassBreak`, `Panel Glass Break` | `alarm_glass_break` |

**Currently unsupported sensor types** (no driver yet, deliberately listed in `UnsupportedZoneTypes` in `lib/ZoneTypes.ts`): freeze, heat, high temperature, shock, doorbell, auxiliary pendant, key fob, siren, tamper, temperature, translator, unknown. The exhaustiveness test in `test/zone-driver-filter.test.ts` will fail if a new `ZoneSensorType` enum value is added without categorisation, forcing a deliberate decision (claim it with a new driver, exclude it, or document it as unsupported) — no silent fall-through.

All zones also get `alarm_tamper` + `alarm_battery`. PowerG zones additionally get `measure_temperature` + `measure_luminance` added at pair time. Keypad / Bluetooth / TakeoverModule types are filtered out entirely.

The pairing flow for each driver shows only the zones it claims. The exclusivity invariant — every includable type claimed by exactly one driver — is enforced by `test/zone-driver-filter.test.ts`.

### Why per-type drivers (and how they share code)
Splitting one "Zone Sensor" driver into six per-type drivers gives users distinct icons in the device list and a clearer pairing flow ("Add Smoke Detector" instead of "Add Zone Sensor → choose from list"). The downside is six driver folders.

To avoid duplicating logic across them, the actual implementation lives in two shared classes:

- **`lib/ZoneDevice.ts`** — handles all device-level behaviour (binding to the shared MQTT client, listening for zone updates, mapping `ZoneStatus` → primary alarm capability, tamper/battery, PowerG temperature + luminance). The class finds the device's primary capability dynamically via `getCapabilities()` rather than baking it in. Each `drivers/<type>/device.ts` is just `module.exports = ZoneDevice`.
- **`lib/ZoneDriver.ts`** — handles the pairing flow + tampered flow card. Subclasses implement `claimsZoneType(t)` to declare their type filter; everything else is generic.

Adding a new per-type driver is therefore: a 4-line `driver.ts` + a 3-line `device.ts` + a `driver.compose.json` declaring its capabilities + a `driver.flow.compose.json` with a `<driver-id>_tampered` trigger.

### Flow card ID uniqueness
Athom requires every flow card id to be globally unique across the entire app. Six drivers all using the same `zone_tampered` id fails validation. Each driver's tampered trigger is therefore named `<driver-id>_tampered` (e.g. `contact-sensor_tampered`, `motion-sensor_tampered`). `ZoneDriver.tamperedFlowCardId` derives this from `this.id` so subclasses don't need to repeat it.

Active statuses: `Open`, `Active`, `Activated`, `Alarmed`, `Occupied`

### Partition State Mapping
| Panel Status | Homey `homealarm_state` |
|---|---|
| `ARM-AWAY` | `armed` |
| `ARM-STAY`, `ARM-NIGHT` | `partially_armed` |
| `DISARM` | `disarmed` |
| `*-EXIT-DELAY` | (no change — arming in progress) |

### Flow Cards
**Alarm Panel triggers:** `alarm_triggered` (tokens: partition_name, alarm_type)
**Alarm Panel actions:** `arm_away`, `arm_home`, `disarm`
**Zone driver triggers (one per driver, all globally unique):** `contact-sensor_tampered`, `motion-sensor_tampered`, `smoke-detector_tampered`, `co-detector_tampered`, `water-sensor_tampered`, `glass-break-detector_tampered`

Standard Homey contact/motion triggers work automatically via capabilities.

### Device Boot Reconnection
On Homey reboot, `AlarmPanelDevice.onInit()` checks for an existing client. If none, it fetches a fresh local IP via `homey.cloud.getLocalAddress()` and creates + connects a new client using the stored panel IP.

---

## Homey SDK Notes

- **No DiscoveryStrategy** — pairing is reversed (panel connects to Homey), so we use custom pair views
- **mDNS advertising** — not built into Homey SDK. Using `bonjour-service` npm package (dgram UDP multicast)
- **TLS server sockets** — work on Homey Pro (proven by MQTT Server app)
- **Settings prefix `zone_`** — reserved by Homey. Use `sensor_zone_id` instead
- **User code** — stored in `homey.settings` key `user_code`, entered during pairing

### IQ Remote Slots
- **IQ Panel 2/2+**: up to 3 virtual remotes
- **IQ Panel 4 / IQ Pro**: up to 8 virtual remotes

Lost certs = lost slot. Certs in `homey.settings` survive app updates but not app uninstall.

---

## Dependencies
- `mqtt` — MQTT client (pure JS)
- `bonjour-service` — mDNS advertisement for pairing
- `node-forge` — PKI/certificate generation
- `uuid` — request ID generation

---

## Out of Scope
- Z-Wave devices enrolled in the panel (enroll into Homey directly)
- Panel TTS
- Alarm.com / cloud features
- Panel scenes
- Multiple user code management
- Weather forecast feed
- PowerG extended capabilities (measure_temperature, measure_luminance) — data is available but not yet wired to capabilities
